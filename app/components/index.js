let Components = {
    Main: require('./main'),
    Hello: require('./hello'),
    Counter: require('./counter'),
    Diff: require('./diff'),
    Lifecycle: require('./lifecycle')
};

module.exports = Components;
